#include "../main.h"

void process_expansion(char **input)
{
    int i;

    if (!input || !*input)
        return;
    i = 0;
    while (*input[i])
    {
        if (*input[i] == '$')
        {
                }
        i++;
    }
}
